<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
   <a href="/">Home</a> 

   <form action="/add-new" method="post">
    <input type="text" name=" title" placeholder="write title">
    <% if (err) { %>
       <p> <%= err['title'] %></p>
        <% } %>
    <textarea name="desc" placeholder="article"></textarea>
    <% if (err) { %>
        <p> <%= err['desc'] %></p>
         <% } %>
    <button>Submit</button>
   </form>

</body>
</html>